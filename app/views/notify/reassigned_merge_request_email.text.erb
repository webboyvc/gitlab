重新指派合并请求 <%= @merge_request.iid %>

<%= url_for([@merge_request.project.namespace.becomes(Namespace), @merge_request.project, @merge_request, { only_path: false }]) %>

指派者 <%= "由 #{sanitize_name(@previous_assignee.name)}" if @previous_assignee -%>
 变更为 <%= "#{@merge_request.assignee_id ? sanitize_name(@merge_request.assignee_name) : '未指派'}" %>
